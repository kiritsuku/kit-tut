```java
package edu.kit.list;

/**
 * The number of stars in brackets defines the difficulty of an operation.
 */
public class IntListProblems {

    /**
     * (*) Find the last element of a list
     */
    public static int last(IntList list) {
        throw new UnsupportedOperationException();
    }

    /**
     * (*) Reverse a list.
     */
    public static IntList reverse(IntList list) {
        throw new UnsupportedOperationException();
    }

    /**
     * (*) Find out whether a list is a palindrome.
     */
    public static boolean isPalindrome(IntList list) {
        throw new UnsupportedOperationException();
    }

    /**
     * (*) Duplicate the elements of a list.
     */
    public static IntList duplicate(IntList list) {
        throw new UnsupportedOperationException();
    }

    /**
     * (**) Duplicate the elements of a list a given number of times.
     */
    public static IntList duplicateN(int n, IntList list) {
        throw new UnsupportedOperationException();
    }

    /**
     * (**) Eliminate consecutive duplicates of list elements. If a list
     * contains repeated elements they should be replaced with a single copy of
     * the element. The order of the elements should not be changed.
     */
    public static IntList compress(IntList list) {
        throw new UnsupportedOperationException();
    }

    /**
     * (**) Pack consecutive duplicates of list elements into sublists. If a
     * list contains repeated elements they should be placed in separate
     * sublists.
     */
    public static IntList[] pack(IntList list) {
        throw new UnsupportedOperationException();
    }

    /**
     * (**) Drop every Nth element from a list.
     */
    public static IntList drop(int n, IntList list) {
        throw new UnsupportedOperationException();
    }

    /**
     * (**) Extract a slice from a list. Given two indices, I and K, the slice
     * is the list containing the elements from and including the Ith element up
     * to but not including the Kth element of the original list. Start counting
     * the elements with 0. On negative indices there is an empty list returned.
     */
    public static IntList slice(int i, int k, IntList list) {
        throw new UnsupportedOperationException();
    }

    /**
     * (**) Rotate a list N places to the left.
     */
    public static IntList rotate(int n, IntList list) {
        throw new UnsupportedOperationException();
    }
}
```